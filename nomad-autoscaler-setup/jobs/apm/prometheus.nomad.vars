# APM: Prometheus
# Requires: prometheus backend job running (make deploy-backend APM=prometheus)
# Deploy:   make deploy-webapp APM=prometheus

apm_source   = "prometheus"
apm_query    = "max_over_time(nomad_client_allocs_cpu_total_percent{task='web'}[1m])"
scale_target = 50
